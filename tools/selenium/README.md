To run selenium tests(using firefox browser) for nanotrader app, please follow these steps:

    cd nanotrader/tools
    ./gradlew build
    cd build/libs
    unzip selenium.zip
    ./runSeleniumTest
